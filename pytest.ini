[pytest]
# Put the repo root on sys.path so `import api.*` works regardless of how
# pytest is invoked (bare `pytest` vs `python -m pytest`). Without this, the
# console-script `pytest` in CI failed to import the `api` package.
pythonpath = .
testpaths = tests
